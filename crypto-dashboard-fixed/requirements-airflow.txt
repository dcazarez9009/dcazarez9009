# Minimal runtime deps required by DAGs and imports (do NOT include apache-airflow here;
# base image already provides Airflow)
pandas==2.0.3
requests==2.31.0
psycopg2-binary==2.9.7
python-dotenv==1.0.0
pyspark==3.5.0
